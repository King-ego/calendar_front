import styled from 'styled-components';

export const Container = styled.div`
  height: 100vh;
  z-index: 999;
  background: rgba(0, 0, 0, 0.2);
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
`;
